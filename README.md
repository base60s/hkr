# HKr — landing

Static landing page for HKr (Chroma Labs). Single `index.html`, no build step.

Deployed to GitHub Pages by `.github/workflows/pages.yml` on every push to `main`.
Live at: https://base60s.github.io/hkr/
